package org.kkpa.algorithms.sorting;

/**
 * Try to use your custom linked list implementation.
 */
public class LinkedMergeSort {


  /**
   * Sorts a linked list in ascending order.
   * Recursively divide the linked list into sublists containing a single node.
   * Repeatedly merge the sublists to produce sorted sublists until one remains.
   *
   * @param myLinkedList Returns a sorted list.
   */
  public static void mergeSort(Object myLinkedList) {

  }
}
